---
lc:
  hosts:
    host:
      ansible_host:
      mirror_root: # path to root directory of mirrors
      proxy_letsencrypt: # path to letsencrypt directory
      proxy_dhparam: # path to dhparams file
  vars:
    tailscale_authkey: # authkey for all hosts in this group
